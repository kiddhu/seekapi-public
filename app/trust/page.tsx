import { CTA, Eyebrow, PageShell, SectionTitle } from '@/components/site';
export const metadata={title:'Trust, Data & Commercial Roles',description:'How SeekAPI handles authorization, data, evidence and service roles.'};
const items=[['Role clarity','Before work begins, you should know whether SeekAPI is acting as a service provider or as the seller of goods.'],['Data control','Internal review is not external forwarding. Named recipients, file/version and purpose require the appropriate authorization.'],['Evidence gate','Customer names, partner claims, service levels and performance metrics remain unpublished until evidence exists.'],['Quality evidence','Inspection or sample evidence is scoped. It does not transfer every design or regulatory responsibility.'],['Third-party costs','Inspection, freight, samples or labs should be approved and distinguished from SeekAPI service fees.'],['Capability truth','A payment method, API or integration is shown as live only after it is actually available.']];
export default function Page(){return <PageShell><section className="page-hero"><div className="container"><Eyebrow>Trust model</Eyebrow><h1>Control should survive the handoff.</h1><p>SeekAPI is designed to make roles, permissions and evidence visible. Trust should come from real boundaries and accountable execution — not a larger list of claims.</p></div></section><section className="section"><div className="container"><SectionTitle eyebrow="Six rules" title="The relationship should be inspectable."/><div className="trust-list">{items.map((item,i)=><div className="trust-item" key={item[0]}><span className="trust-num">{i+1}</span><div><h3>{item[0]}</h3><p>{item[1]}</p></div></div>)}</div></div></section><section className="section section-dark"><div className="container"><SectionTitle eyebrow="Data ladder" title="Share only what the next decision needs." body="The final manufacturing and human-execution data policy will be published only after the storage and forwarding path is verified."/><div className="grid-3"><div className="callout"><h3>Discuss</h3><p>Problem, use case and non-sensitive information.</p></div><div className="callout"><h3>Internal review</h3><p>A limited specification may be reviewed without permission to forward it externally.</p></div><div className="callout"><h3>External RFQ</h3><p>Named recipient, approved file or revision, purpose and scope.</p></div></div></div></section><CTA title="The first trust test should be small." body="Start with one non-sensitive issue and expand permission only when the next step requires it."/></PageShell>}
